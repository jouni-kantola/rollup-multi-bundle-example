# rollup-multi-bundle-example

Quick and dirty multi bundle example with rollup (using [rollem](https://github.com/bahmutov/rollem))

## Usage
1. `yarn install` / `npm install`
2. `node_modules/.bin/rollem`
